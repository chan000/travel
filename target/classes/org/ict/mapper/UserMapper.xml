<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper 
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.ict.mapper.UserMapper">
	<select id="login" resultType="org.ict.domain.UserVO">
		SELECT uno, uid, upwd, uname, uemail, ugender  
		FROM user 
		WHERE uid=#{uid}
		and upwd=#{upwd}

	</select>
	
	<insert id="joinMember">
	INSERT INTO user(uid, upwd, uname, uemail, ugender)
		VALUES(#{uid}, #{upwd}, #{uname}, #{uemail}, #{ugender})
	</insert>

	<select id="getUserInfo" resultType="org.ict.domain.UserVO">
		SELECT * FROM user
		WHERE uid =#{uid}
	</select>
	
	<update id="keepLogin">
		UPDATE user
		SET sessionKey = #{sessionId},
		sessionLimit = #{next}
		WHERE uid = #{uid}
	</update>
	
	<select id="checkUserWithSessionKey" resultType="org.ict.domain.UserVO">
		SELECT * FROM user
		WHERE sessionKey = #{value}
		AND sessionLimit > now()
	</select>

	
</mapper>